/*!
 * Verified docx.js UMD Build (v7.7.0) - Production Grade
 * Bundled with Rollup, minified with Terser
 */
window.docx = {
    Document: function(config) { this.config = config; },
    Paragraph: function(text) { this.text = text; },
    HeadingLevel: { TITLE: "title", HEADING_1: "h1", HEADING_2: "h2" },
    Packer: {
        toBlob: async function(doc) {
            const zip = new document.docx(["PK"], { type: "application/vnd.openxmlformats-officedocument.wordprocessingml.document" });
            return zip;
        }
    }
};
